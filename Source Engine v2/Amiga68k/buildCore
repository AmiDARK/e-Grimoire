Clear
Echo *************************************************
Echo * Grimoire Build-Core libraries script Ver 0.1a *
Echo *************************************************
Echo Credits : Frederic Cordier Aka AmiDARK
Echo Date : 2022.04.26
Echo " "

; ******************************** Compiles grimoire-core.library ********************************
Echo " "
SET sourceName src/coreLib/coreLib.asm
SET compName System/grimoire-core.library
SET labelName grimoire-core.library
SET logsName logs/grimoire-core.logs
Echo "============> Build : $labelName"
; Build : $labelName
IF EXISTS $compName
  Delete $compName >NIL:
  Echo previous '$labelName' deleted
ENDIF
Echo Compiles '$labelName'
FAILAT 31
c/genam FROM $sourceName TO $compName >$logsName
SET temp1 $RC
IF VAL $temp1 EQ 0
  Echo Library '$labelName' compiled successfully
  Delete $logsName >NIL:
ELSE
  Echo Library '$labelName' returned compilation error(s), please check $logsName file for details.
  QUIT
ENDIF

; ******************************** Compiles grimoire-fpconvert.library ********************************
Echo " "
SET sourceName src/fpuLib/fpuLib.asm
SET compName System/grimoire-fpconvert.library
SET labelName grimoire-fpconvert.library
SET logsName logs/grimoire-fpconvert.logs
Echo "============> Build : $labelName"
; Build : $labelName
IF EXISTS $compName
  Delete $compName >NIL:
  Echo previous '$labelName' deleted
ENDIF
Echo Compiles '$labelName'
FAILAT 31
c/genam FROM $sourceName TO $compName >$logsName
IF VAL $RC EQ 0
  Echo Library '$labelName' compiled successfully
  Delete $logsName >NIL:
ELSE
  Echo Library '$labelName' returned compilation error(s), please check $logsName file for details.
  QUIT
ENDIF

; ******************************** Compiles grimoire-hardwareDetector.library ********************************
Echo " "
SET sourceName src/hardwareDetector/hardwareDetector.asm
SET compName System/grimoire-hardwareDetector.library
SET labelName grimoire-hardwareDetector.library
SET logsName logs/grimoire-hardwareDetector.logs
Echo "============> Build : $labelName"
; Build : $labelName
IF EXISTS $compName
  Delete $compName >NIL:
  Echo previous '$labelName' deleted
ENDIF
Echo Compiles '$labelName'
FAILAT 31
c/genam FROM $sourceName TO $compName >$logsName
IF VAL $RC EQ 0
  Echo Library '$labelName' compiled successfully
  Delete $logsName >NIL:
ELSE
  Echo Library '$labelName' returned compilation error(s), please check $logsName file for details.
  QUIT
ENDIF

; ******************************** Compiles grimoire-ScreensFullSaga.library ********************************
Echo " "
SET sourceName src/screens/ScreensFullSAGA.asm
SET compName System/grimoire-screensFullSaga.library
SET labelName grimoire-screensFullSaga.library
SET logsName logs/grimoire-screensFullSaga.logs
Echo "============> Build : $labelName"
; Build : $labelName
IF EXISTS $compName
  Delete $compName >NIL:
  Echo previous '$labelName' deleted
ENDIF
Echo Compiles '$labelName'
FAILAT 31
c/genam FROM $sourceName TO $compName >$logsName
IF VAL $RC EQ 0
  Echo Library '$labelName' compiled successfully
  Delete $logsName >NIL:
ELSE
  Echo Library '$labelName' returned compilation error(s), please check $logsName file for details.
  QUIT
ENDIF

; ******************************** Pre-Compiles grimoire-coldStart.gs ********************************
Echo " "
SET sourceName Src/grimoire-coldStartMacros.asm
SET compName System/grimoire-coldStart.gs
SET labelName grimoire-coldStart.gs
SET logsName logs/grimoire-coldStart.gs.logs
Echo "============> Build : $labelName"
; Build : $labelName
IF EXISTS $compName
  Delete $compName >NIL:
  Echo previous '$labelName' deleted
ENDIF
Echo Compiles '$labelName'
FAILAT 31
c/genam FROM $sourceName TO $compName >$logsName
IF VAL $RC EQ 0
  Echo Library '$labelName' compiled successfully
  Delete $logsName >NIL:
ELSE
  Echo Library '$labelName' returned compilation error(s), please check $logsName file for details.
  QUIT
ENDIF


Echo " "

